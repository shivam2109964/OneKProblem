void main() {
  Iterable<String> iterable = ['apple', 'banana', 'orange'];
  print(iterable);
  print(iterable.first);
  print(iterable.last);
  print(iterable.length);
  print(iterable.indexed);
  print(iterable.iterator);
}
